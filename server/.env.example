GAME_DATA_DIR="./assets/games/"
PORT=8080
DB_PASSWORD="changeme"
# Must match docker-compose db port (5432), not the container-internal port
DATABASE_URL="postgresql://nhie:changeme@localhost:5432/nhie"

# Drizzle CLI & seeder use separate env files (copy the examples):
#   server/.env.local       → pnpm db:push, db:migrate, db:studio, db:seed
#   server/.env.production  → pnpm db:push:prod, db:migrate:prod, db:seed:prod
# AXIOM_TOKEN=""
# AXIOM_ORG_ID=""

DEBUG=true